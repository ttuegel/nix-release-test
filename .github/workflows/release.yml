name: "Release"
on:
  release:
    types:
      - created

jobs:
  nix-release:
    name: 'Release Nix expression'
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2.3.4
        with:
          submodules: recursive

      - name: Upload lock file
        uses: ttuegel/upload-release.nix@c00b12b93a4c37158fcaaf2f0b7701877107c9ce
